@model SimpleBillingApp.Models.Article

@{
    ViewBag.Title = "Edit";
}

<h2>Edit Article</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Article</legend>

        @Html.HiddenFor(model => model.articleId)

        <div class="editor-label">
            @Html.LabelFor(model => model.articleName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.articleName)
            @Html.ValidationMessageFor(model => model.articleName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.articlePrice)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.articlePrice)
            @Html.ValidationMessageFor(model => model.articlePrice)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
